import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mini Games Collection',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
